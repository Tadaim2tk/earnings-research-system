"""Reading cohort outcomes without letting one name speak for the group.

`cohort` turns a list of returns into what a decision actually needs: how often
it worked, where the middle sits, how much rests on a single name, and whether
any of it is distinguishable from chance.
"""
